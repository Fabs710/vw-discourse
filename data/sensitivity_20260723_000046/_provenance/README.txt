Batch-level provenance snapshot (24 Jul 2026): the exact scenario text and the six generated
agent briefs used by all 11 baseline conditions of this batch (identical across conditions;
condition-specific parameter overrides are in each condition's config_used.yaml).
From the repeat batch onward, every run folder saves its own scenario_used + brief_* files.
