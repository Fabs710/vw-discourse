# Secrets - never commit
.env
# Python
__pycache__/
*.pyc
.venv/
venv/
# data/ is PUBLISHED with the repository: the final data state of the 141 controlled
# runs + validation + audits is part of the reproducibility record.
# OS / editor
.DS_Store
